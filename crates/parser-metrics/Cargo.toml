[package]
name = "parser-metrics"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
parser-core = { path = "../parser-core" }
parser-sfdc-benchmarks = { path = "../parser-sfdc-benchmarks" }
serde = { version = "1", features = ["derive"] }
