[package]
name = "parser-sfdc-benchmarks"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
parser-core = { path = "../parser-core" }
serde = { version = "1", features = ["derive"] }
